#pragma once

#include <algorithm>
#include <iostream>
#include <vector>

namespace Answers::Arrays {
// Q1
std::vector<int> CountSubarrays(std::vector<int> arr);
// Q2
std::vector<int> FindSignatureCounts(std::vector<int> arr);
} // namespace Answers::Arrays
